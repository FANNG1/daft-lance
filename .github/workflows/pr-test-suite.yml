name: PR Test Suite

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  style:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      UV_PYTHON: "3.10"
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          cache-dependency-glob: |
            pyproject.toml
            uv.lock
      - uses: actions/cache@v4
        with:
          path: ~/.cache/pre-commit
          key: ${{ runner.os }}-python-${{ env.UV_PYTHON }}-pre-commit-${{ hashFiles('.pre-commit-config.yaml') }}
      - name: Run style checks
        run: make precommit

  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - name: Run tests
        run: uv run --python ${{ matrix.python-version }} pytest tests/ -v

  test-ray:
    runs-on: ubuntu-latest
    env:
      DAFT_RUNNER: ray
      DAFT_PROGRESS_BAR: "0"
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - name: Run blob tests on Ray
        run: uv run --python 3.12 --group ray pytest tests/io/lance/test_lance_blob_v2_read.py -v
